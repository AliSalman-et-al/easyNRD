% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readmission_linkage.R
\name{nrd_track_outcomes}
\alias{nrd_track_outcomes}
\title{Track Competing-Risk Outcomes from Episode-Level NRD Data}
\usage{
nrd_track_outcomes(.data, index_condition, window = 30L)
}
\arguments{
\item{.data}{Episode-level lazy table from \code{\link[=nrd_assemble_episodes]{nrd_assemble_episodes()}}.}

\item{index_condition}{Data-masked expression defining index episodes.}

\item{window}{Integer readmission window in days. Defaults to \code{30L}.}
}
\value{
Full input table with appended \code{IndexEvent}, \code{Days_to_End_of_Year},
\code{time_to_event}, and \code{outcome_status} (character).
}
\description{
Creates denominator-preserving survival variables for competing-risk analyses
after episode assembly.
}
\details{
This function is purpose-built for survival modeling workflows where the
nationwide denominator must remain intact for survey-based variance
estimation. No global exclusions are applied.

Methodological sequence:
\itemize{
\item evaluate \code{index_condition} to create \code{IndexEvent}
\item compute right-censoring support (\code{Days_to_End_of_Year}) using
\code{Episode_DMONTH} and a 15th-of-month discharge approximation
\item identify first valid readmission within \code{window} days by many-to-many
self-join on \code{YEAR} + \code{NRD_VISITLINK}, linking index discharge to candidate
admission
\item generate compact survival outputs only (no wide candidate payload)
}

HCUP-NRD linkage is constrained to a single calendar year; this function
respects that architecture by joining only within \code{YEAR}.

\code{outcome_status} is returned as a character column to ensure reliable SQL
translation on lazy backends. After \code{collect()}, users should cast to factor
with levels:
\enumerate{
\item \code{"Died at Index"}
\item \code{"Readmitted"}
\item \code{"Censored"}
}
}
\examples{
\donttest{
episodes <- tibble::tibble(
  YEAR = c(2019L, 2019L),
  NRD_VISITLINK = c(1L, 1L),
  Episode_ID = c(1L, 2L),
  Episode_KEY_NRD = c(1001L, 1002L),
  Episode_Admission_Day = c(10, 25),
  Episode_Discharge_Day = c(12, 30),
  Episode_DMONTH = c(1L, 1L),
  DIED = c(0L, 0L)
)
outcomes <- episodes |>
  nrd_track_outcomes(index_condition = Episode_ID >= 1, window = 30L)
}
}
