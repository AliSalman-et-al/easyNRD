% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readmission_linkage.R
\name{nrd_extract_readmissions}
\alias{nrd_extract_readmissions}
\title{Flag First Readmission Episodes for Secondary Clinical Profiling}
\usage{
nrd_extract_readmissions(.data, index_condition, window = 30L)
}
\arguments{
\item{.data}{Episode-level lazy table from \code{\link[=nrd_assemble_episodes]{nrd_assemble_episodes()}}.}

\item{index_condition}{Data-masked expression defining index episodes.}

\item{window}{Integer readmission window in days. Defaults to \code{30L}.}
}
\value{
Full input table with appended integer flags
\code{Is_Readmission_Event} and \code{Linked_To_Target_Index}.
}
\description{
Identifies first qualifying readmission episodes linked to a target index
cohort and appends extraction flags to the full denominator-preserving table.
}
\details{
This module intentionally separates clinical profiling from survival outcome
construction. It performs the same temporal linkage used in
\code{\link[=nrd_track_outcomes]{nrd_track_outcomes()}} and marks only episodes that are true first
readmissions for the index cohort.

Returned flags:
\itemize{
\item \code{Is_Readmission_Event}: row is a first readmission episode
\item \code{Linked_To_Target_Index}: row belongs to the first-readmission pool linked
to the index cohort defined by \code{index_condition}
}

Because the full lazy table is returned without dropping rows, users can
construct complex survey designs first, then subset analytically.
}
\examples{
\donttest{
episodes <- tibble::tibble(
  YEAR = c(2019L, 2019L),
  NRD_VISITLINK = c(1L, 1L),
  Episode_ID = c(1L, 2L),
  Episode_KEY_NRD = c(1001L, 1002L),
  Episode_Admission_Day = c(10, 25),
  Episode_Discharge_Day = c(12, 30),
  Episode_DMONTH = c(1L, 1L),
  DIED = c(0L, 0L)
)
flagged <- episodes |>
  nrd_extract_readmissions(index_condition = Episode_ID >= 1, window = 30L)
}
}
