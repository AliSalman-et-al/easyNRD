% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label.R
\name{nrd_label}
\alias{nrd_label}
\title{Translate HCUP Numeric Codes to Character Labels}
\usage{
nrd_label(.data, ..., .keep_original = FALSE)
}
\arguments{
\item{.data}{A data frame, lazy table, or Arrow query.}

\item{...}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Columns to label.
If omitted, all columns present in \code{.data} that have dictionary mappings
are labeled automatically.}

\item{.keep_original}{Logical scalar. If \code{FALSE} (default), overwrite the
selected coded columns. If \code{TRUE}, keep the original coded columns and
append labeled columns with the \verb{_label} suffix.}
}
\value{
A modified object of the same backend class as \code{.data}.
}
\description{
\code{nrd_label()} translates standard HCUP integer-coded variables into
human-readable character labels using dictionary mappings stored in
\code{easyNRD} internals. For lazy database backends, translations are expressed
with \code{dplyr::case_match()} so they push down to SQL \verb{CASE WHEN} operations
and run out-of-core before materialization.
}
\examples{
if (FALSE) {
  nrd_ingest("/path/to/nrd.parquet") |>
    nrd_select(FEMALE, PAY1, AGE) |>
    nrd_label(FEMALE, PAY1) |>
    nrd_as_survey()
}
}
