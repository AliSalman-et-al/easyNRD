% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_nrd_base_data.R
\name{nrd_assemble_episodes}
\alias{nrd_assemble_episodes}
\title{Assemble NRD Clinical Episodes from Raw Hospitalization Rows}
\usage{
nrd_assemble_episodes(
  datasets,
  unify_schemas = TRUE,
  transfer_disp_codes = 2L,
  transfer_sameday_codes = c(1L, 4L)
)
}
\arguments{
\item{datasets}{A parquet path, Arrow dataset, or lazy table.}

\item{unify_schemas}{Logical passed to \code{arrow::open_dataset()} when
\code{datasets} is a parquet path.}

\item{transfer_disp_codes}{Integer vector of \code{DISPUNIFORM} values indicating
transfer-out events. Defaults to \code{2L}.}

\item{transfer_sameday_codes}{Integer vector of \code{SAMEDAYEVENT} values
indicating transfer-network continuity. Defaults to \code{c(1L, 4L)}.}
}
\value{
A lazy episode-level table.
}
\description{
Ingests HCUP-NRD parquet data lazily, augments administrative labels, extracts
diagnosis/procedure code strings, and deterministically collapses transfer-
linked rows into clinical episodes.
}
\details{
HCUP-NRD is a hospitalization-level administrative dataset. Transfer networks
can create multiple rows for one clinical episode; therefore, episode
consolidation is mandatory before any readmission or survival modeling.

The consolidation procedure is SQL-safe for lazy backends and uses only
deterministic ordering plus stable aggregates:
\itemize{
\item grouping frame: \code{YEAR} + \code{NRD_VISITLINK}
\item temporal frame: \code{dbplyr::window_order(NRD_DAYSTOEVENT, KEY_NRD)}
\item chain continuity requires transfer disposition and same-day network codes,
with exact day-to-day adjacency between prior discharge and current
admission
\item \code{Episode_Index_KEY_NRD} and \code{Episode_KEY_NRD} are broadcast in a windowed
mutate before summarization; \code{dplyr::first()}/\code{dplyr::last()} are not used
in \code{summarise()}.
}

Final episode variables include:
\itemize{
\item \code{Episode_Admission_Day = min(NRD_DAYSTOEVENT)}
\item \code{Episode_LOS = sum(LOS, na.rm = TRUE)}
\item \code{Episode_Discharge_Day = Episode_Admission_Day + Episode_LOS}
\item \code{DIED = max(DIED, na.rm = TRUE)}
}

This function preserves lazy execution in Arrow/DuckDB workflows and is
designed to support denominator-preserving survey-based analyses.
}
\examples{
\donttest{
if (FALSE) {
  episodes <- nrd_assemble_episodes(
    datasets = "/path/to/nrd/parquet",
    transfer_disp_codes = 2L,
    transfer_sameday_codes = c(1L, 4L)
  )
}
}
}
