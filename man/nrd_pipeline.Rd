% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{nrd_pipeline}
\alias{nrd_pipeline}
\title{Process and pool multi-year NRD analyses safely}
\usage{
nrd_pipeline(paths, .f, output_dir = NULL)
}
\arguments{
\item{paths}{A named character vector or named list of parquet paths. Names
identify the year labels and are processed in the supplied order. \code{easyNRD}
supports NRD data years 2016 and later.}

\item{.f}{A function that takes one ingested single-year DuckDB-backed lazy
table and returns a DuckDB-backed lazy table. The function must not call
\code{collect()} or \code{\link[=nrd_close]{nrd_close()}}.}

\item{output_dir}{Optional directory where per-year parquet intermediates are
written as \verb{NRD_<year>_pipeline.parquet}. If \code{NULL}, \code{nrd_pipeline()} uses a
temporary intermediate parquet directory.}
}
\value{
Invisibly returns a pooled DuckDB-backed \code{tbl_lazy}. The caller must
call \code{\link[=nrd_close]{nrd_close()}} on the returned object.
}
\description{
\code{nrd_pipeline()} runs a user-supplied single-year lazy pipeline independently
for each named NRD parquet path, exports per-year results, then re-ingests the
per-year outputs as one pooled DuckDB-backed lazy table.
}
\details{
\code{easyNRD} supports NRD data years 2016 and later only. Year 2016 is the first
NRD release with a full calendar year of ICD-10-CM/PCS data and the
\verb{I10_DX*}/\verb{I10_PR*} column naming convention used throughout the package. The
2015 NRD mixes nine months of ICD-9-CM with three months of ICD-10-CM/PCS and
uses a different file structure. Earlier years use ICD-9-CM and different
column naming entirely.

Each element of \code{paths} is ingested, processed, exported, and closed in full
before the next year begins. Pooling happens only by re-ingesting the
exported per-year parquet files after all yearly pipelines complete. This is a
row union after yearly processing, not a cross-year join of raw NRD data.

This contract makes cross-year readmission linkage structurally impossible.
\code{NRD_VISITLINK}, \code{HOSP_NRD}, and \code{NRD_STRATUM} are meaningful only within a
single NRD year, so user code in \code{.f} always sees one year at a time.

Do not call \code{\link[=nrd_as_survey]{nrd_as_survey()}} on the pooled output. NRD survey design is
year-specific, and valid weighted analysis requires computing year-specific
estimates on single-year outputs and combining those estimates afterward.

\code{nrd_pipeline()} makes no attempt to deduplicate or re-key \code{KEY_NRD} across a
pooled result. \code{YEAR} remains required to disambiguate records in any pooled
table.

When \code{output_dir = NULL}, \code{nrd_pipeline()} writes per-year outputs to a
temporary intermediate parquet directory. That directory is deleted if the
pipeline errors, and it is also deleted when \code{\link[=nrd_close]{nrd_close()}} is called on the
pooled object. After \code{\link[=nrd_close]{nrd_close()}} is called, the pooled table must not be
queried further. DuckDB session temp directories are managed separately by the
existing \code{\link[=nrd_ingest]{nrd_ingest()}} / \code{\link[=nrd_close]{nrd_close()}} owner mechanism.
}
\examples{
if (FALSE) {
  pooled <- nrd_pipeline(
    c(
      "2018" = "/path/to/nrd_2018.parquet",
      "2019" = "/path/to/nrd_2019.parquet"
    ),
    function(data) {
      data |>
        nrd_prepare() |>
        dplyr::filter(DIED == 0L)
    }
  )

  nrd_close(pooled)
}
}
