% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/materialize.R
\name{nrd_materialize}
\alias{nrd_materialize}
\title{Materialize a Lazy NRD Pipeline Checkpoint}
\usage{
nrd_materialize(data, name = "m_cohort", path = NULL, row_group_size = 100000L)
}
\arguments{
\item{data}{A DuckDB-backed lazy table produced by \code{easyNRD} pipeline steps.}

\item{name}{Table name used when materializing in-database with
\code{\link[dplyr:compute]{dplyr::compute()}}. Defaults to \code{"m_cohort"}.}

\item{path}{Optional parquet output path. If \code{NULL} (default), materializes
to a DuckDB table with \code{\link[dplyr:compute]{dplyr::compute()}}. If provided, exports to parquet
and re-ingests the file into a fresh lazy DuckDB table.}

\item{row_group_size}{Target parquet row-group size used when \code{path} is
provided. Smaller groups can reduce peak memory pressure for wide tables.
Defaults to \code{100000L}.}
}
\value{
A DuckDB-backed lazy table (\code{tbl_lazy}) pointing to the materialized
checkpoint, suitable for direct piping to \code{\link[=nrd_link_readmissions]{nrd_link_readmissions()}} or
\code{\link[=nrd_as_survey]{nrd_as_survey()}}.
}
\description{
\code{nrd_materialize()} forces evaluation of a lazy NRD pipeline and returns a
new lazy DuckDB table that can be chained into downstream steps. Use this as
a checkpoint after regex-heavy phenotyping across many diagnosis fields, and
before episode construction or readmission linkage.
}
\examples{
\donttest{
if (FALSE) {
  checkpoint <- nrd_ingest("/path/to/nrd.parquet") |>
    dplyr::mutate(ami_flag = stringr::str_detect(Episode_DX10, "I21")) |>
    nrd_materialize(name = "m_ami")

  checkpoint_disk <- checkpoint |>
    nrd_materialize(path = "/path/to/checkpoints/ami.parquet")
}
}
}
\seealso{
Other pipeline functions: 
\code{\link{nrd_build_episodes}()},
\code{\link{nrd_cleanup}()},
\code{\link{nrd_ingest}()},
\code{\link{nrd_link_readmissions}()}
}
\concept{pipeline functions}
